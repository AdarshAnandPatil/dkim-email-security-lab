const slides=[
['DOMAINKEYS IDENTIFIED MAIL','DKIM\nEmail Authentication and Protection Against Tampering'],['EMAIL SECURITY PROBLEM','Email can be spoofed, altered, or impersonated.\n\nHow can a receiver evaluate a domain signature?'],['EMAIL SPOOFING','An attacker may pretend to send mail from a trusted domain. DKIM adds cryptographic evidence.'],['WHAT IS DKIM?','DomainKeys Identified Mail is an email authentication method using a cryptographic signature and a DNS public key.'],['DKIM COMPONENTS','📧 Sender\n🔐 DKIM Signer\n🔑 Private Key\n✍️ DKIM-Signature\n🌐 DNS Public Key\n🛡️ Receiving Server'],['DKIM ARCHITECTURE','SENDER\n↓\nCREATE EMAIL\n↓\nDKIM SIGNING → PRIVATE KEY\n↓\nDKIM SIGNATURE\n↓\nINTERNET\n↓\nRECEIVER → DNS LOOKUP → PUBLIC KEY\n↓\nVERIFICATION → PASS / FAIL'],['STEP 1 — CREATE EMAIL','Subject: Important Examination Notice\nMessage: Your examination schedule is available.'],['STEP 2 — CANONICALIZATION','Selected headers and body content are prepared in a consistent representation before signing.'],['STEP 3 — SHA-256 HASHING','Original Message\n↓\nSHA-256\n↓\nMessage Hash'],['STEP 4 — PRIVATE KEY SIGNING','Message Hash + Private Key\n↓\nDKIM Signature'],['STEP 5 — SIGNATURE ATTACHED','The DKIM-Signature information is added to the email header.'],['STEP 6 — DNS RECORD','demo._domainkey.college-example.com\n↓\nPublic Key'],['STEP 7 — EMAIL TRAVELS','The signed email travels through the Internet to the receiver.'],['STEP 8 — RECEIVER READS SIGNATURE','The receiver identifies the signing domain and selector.'],['STEP 9 — DNS LOOKUP','The receiver queries DNS and obtains the corresponding public key.'],['STEP 10 — VERIFICATION','Received Email + DKIM Signature + DNS Public Key\n↓\nVerification\n↓\nPASS / FAIL'],['DKIM STRATEGY','Sign selected content. Publish the public key in DNS. Let receivers verify integrity.'],['ORIGINAL EMAIL — PASS','Original content remains consistent with the signature.\n\n✅ DKIM = PASS'],['ATTACK SCENARIO','🕵️ SHADOW-X intercepts the email and changes its content.'],['WHY DOES TAMPERING FAIL?','Original Content → Original Hash → Original Signature\n\nModified Content → Different Hash\n\n❌ Signature Does Not Match'],['PASS vs FAIL','✅ PASS: signature verifies for the signed content.\n\n❌ FAIL: signed content changed or verification cannot succeed.'],['SPF + DKIM + DMARC','SPF: authorized sending infrastructure.\nDKIM: domain signatures.\nDMARC: policy and alignment.'],['ADVANTAGES','• Helps detect modification\n• Domain authentication signal\n• DNS public-key distribution\n• Widely used in email systems'],['LIMITATIONS','A valid DKIM signature alone does not guarantee an email is harmless. Correct key and DNS management are required.'],['CONCLUSION','DKIM uses cryptographic signatures and DNS public keys to help detect modification of signed email content.\n\nTHANK YOU 🙏']];
let current=0;function render(){document.getElementById('count').textContent=`Slide ${current+1} / ${slides.length}`;let t=slides[current][1];document.getElementById('slide').innerHTML=`<h1>${slides[current][0]}</h1>`+(t.includes('↓')?`<div class="diagram">${t}</div>`:`<p>${t}</p>`)}function nextSlide(){current=(current+1)%slides.length;render()}function prevSlide(){current=(current+slides.length-1)%slides.length;render()}render();
function go(id){document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));document.getElementById(id).classList.add('active');window.scrollTo({top:0,behavior:'smooth'})}
const events=['01. ARIA Creates Email 📧','02. Email Goes to SIGNER-01 🔐','03. Canonicalization 🔄','04. SHA-256 Hashing #','05. Private Key Signing 🔑','06. DKIM Signature Attached ✍️','07. Public Key in DNS 🌐','08. Email Travels 📡','09. SHADOW-X Attacks 🕵️','10. GUARD-01 Receives 🛡️','11. DNS Lookup 🔍','12. Verification ⚙️','13. Original Email → PASS ✅','14. Modified Email → FAIL ❌'];document.getElementById('timeline').innerHTML=events.map(x=>`<div>${x}</div>`).join('');
async function hash(t){let d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(t));return [...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}
async function simulate(){let d=domain.value,s=selector.value,sub=subject.value,m=message.value,h=await hash(sub+'|'+m),sig=await hash(h+'|SIMULATED_PRIVATE_KEY|'+d);simresult.innerHTML=`<div class="step"><b>Step 1 — Create Email</b><br>${sub}<br>${m}</div><div class="step"><b>Step 2 — SHA-256 Hash</b><div class="hash">${h}</div></div><div class="step"><b>Step 3 — Signing</b><br>Message Hash + Simulated Private Key<div class="hash">${sig}</div></div><div class="step"><b>Step 4 — DNS Record</b><br>${s}._domainkey.${d} → Simulated Public Key</div><div class="result pass"><b>Step 5 — Verification: ✅ DKIM = PASS</b><br>The original demonstration data matches the generated signature.</div>`}
async function attack(){let a=await hash(original.textContent),b=await hash(tampered.value);attackresult.innerHTML=`<div class="step"><b>Original Hash</b><div class="hash">${a}</div></div><div class="step"><b>Modified Hash</b><div class="hash">${b}</div></div><div class="result fail"><h3>❌ DKIM = FAIL</h3><b>⚠️ TAMPERING DETECTED</b><br>The modified content produces different data and cannot match the original demonstration signature.</div>`}
